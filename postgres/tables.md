Создание и удаление таблицы
```sql
CREATE TABLE my_schema.my_table
(
	first varchar,
	second  varchar,
    number varchar,
    comb text,
    other character(1)
);

DROP TABLE my_schema.my_table;
```
